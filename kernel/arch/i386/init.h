#pragma once
#include <stddef.h>

// for now, 2 are fine, im gonna support one anyway lol
enum CPU_ARCH {
    x86 = 1,
    x64 = 2
};

typedef size_t MAIN_MEMORY_LENGTH;
typedef int CPU_CORES;

typedef struct kInfo_t {

} kInfo_t;
